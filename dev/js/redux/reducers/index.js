import { combineReducers } from 'redux';

import Reducer from './Reducer';

const rootReducer = combineReducers({
    Reducer
    // add more reducers here after importing
});

export default rootReducer;